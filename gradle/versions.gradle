// assign all version before we do anything else
subprojects.each{project->
    if(versions[project.name])
        project.version = "${versions[project.name]}"
    else
        project.version = "${version}"
}
